# ==========================================
# CẤU HÌNH MÔI TRƯỜNG STUDYHUB FRONTEND
# ==========================================
# Copy file này thành .env hoặc .env.production khi build lên Production.
# Lưu ý: Khi deploy lên domain thật (ví dụ https://studyhub.vn), VITE_API_BASE_URL phải trỏ về URL thực tế của Backend (ví dụ https://api.studyhub.vn/api/v1).

VITE_API_BASE_URL=http://localhost:8081/api/v1
